syntax = "proto3";

package blanketops.events.v1alpha1;

import "blanketops/environments/common/v1/event.proto";
import "blanketops/environments/common/v1/metadata.proto";
import "google/protobuf/timestamp.proto";

option csharp_namespace = "BlanketOps.Events.V1Alpha1";
option go_package = "github.com/ntlaletsi70/blanketops-environments-contract/blanketops/events/v1alpha1;v1alpha1";
option java_outer_classname = "GitHubEventProto";

// -----------------------------------------------------------------------------
// Resource
// -----------------------------------------------------------------------------

message GitHubEvent {
  blanketops.environments.common.v1.Metadata metadata = 1;
  GitHubEventSpec spec = 2;
  GitHubEventStatus status = 3;
}

// -----------------------------------------------------------------------------
// Spec (EXTERNAL EVENT)
// -----------------------------------------------------------------------------

message GitHubEventSpec {
  string repository = 1;
  GitHubEventType event_type = 2;
  string ref = 3;
  string commit_sha = 4;
  string actor = 5;
  // Idempotency key (GitHub delivery ID)
  string event_id = 6;
  google.protobuf.Timestamp occurred_at = 7;
}

// -----------------------------------------------------------------------------
// Status (SYSTEM PROCESSING)
// -----------------------------------------------------------------------------

message GitHubEventStatus {
  GitHubEventPhase phase = 1;
  string triggered_ref = 2;
  string reason = 3;
  google.protobuf.Timestamp processed_at = 4;
  int64 observed_generation = 5;
}

// -----------------------------------------------------------------------------
// Enums
// -----------------------------------------------------------------------------

enum GitHubEventType {
  GIT_HUB_EVENT_TYPE_UNSPECIFIED = 0;
  GIT_HUB_EVENT_TYPE_PUSH = 1;
  GIT_HUB_EVENT_TYPE_PULL_REQUEST = 2;
  GIT_HUB_EVENT_TYPE_RELEASE = 3;
  GIT_HUB_EVENT_TYPE_MANUAL = 4;
}

enum GitHubEventPhase {
  GIT_HUB_EVENT_PHASE_UNSPECIFIED = 0;
  GIT_HUB_EVENT_PHASE_RECEIVED = 1;
  GIT_HUB_EVENT_PHASE_ACCEPTED = 2;
  GIT_HUB_EVENT_PHASE_REJECTED = 3;
  GIT_HUB_EVENT_PHASE_TRIGGERED = 4;
}
// -----------------------------------------------------------------------------
// Requests / Responses
// -----------------------------------------------------------------------------

message CreateGitHubEventRequest {
  GitHubEventSpec spec = 1;
}

message CreateGitHubEventResponse {
  GitHubEvent github_event = 1;
}

message GetGitHubEventRequest {
  string name = 1;
}

message GetGitHubEventResponse {
  GitHubEvent github_event = 1;
}

message ListGitHubEventsRequest {}

message ListGitHubEventsResponse {
  repeated GitHubEvent github_events = 1;
}

message DeleteGitHubEventRequest {
  string name = 1;
}

message DeleteGitHubEventResponse {}

message WatchGitHubEventRequest {
  string name = 1;
  string label_selector = 2;
}

message WatchGitHubEventResponse {
  GitHubEvent github_event = 1;
  blanketops.environments.common.v1.EventType type = 2;
}

// -----------------------------------------------------------------------------
// Service
// -----------------------------------------------------------------------------

service GitHubEventService {
  rpc CreateGitHubEvent(CreateGitHubEventRequest) returns (CreateGitHubEventResponse);
  rpc GetGitHubEvent(GetGitHubEventRequest) returns (GetGitHubEventResponse);
  rpc ListGitHubEvents(ListGitHubEventsRequest) returns (ListGitHubEventsResponse);
  rpc DeleteGitHubEvent(DeleteGitHubEventRequest) returns (DeleteGitHubEventResponse);
  rpc WatchGitHubEvent(WatchGitHubEventRequest) returns (stream WatchGitHubEventResponse);
}
